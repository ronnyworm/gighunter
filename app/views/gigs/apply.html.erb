<h1>E-Mail an Veranstalter versenden</h1>

<%= form_tag "/apply" do %>
<%= button_tag "Versenden", type: "submit", class: "btn btn-primary" %>
<%= link_to "Zurück", gigs_path, class: "btn btn-default" %>


<div class="table-responsive">
<table class="table table-striped">
<thead>
  <tr>
    <th>Name</th>
    <th>(voraussichtliches) Datum</th>
    <th>E-Mail-Adresse</th>
    <th></th>
  </tr>
</thead>

<tbody>
  <% @relevant_gigs.each do |g| %>
    <tr>
      <td><%= g[:name] %></td>
      <td><%= g[:datetime] %></td>
      <td><%= g[:email] %></td>
      <td><%= link_to "ansehen", show_mail_url(g[:id]) %></td>
    </tr>
  <% end %>
</tbody>
</table>
</div>

<% end %>